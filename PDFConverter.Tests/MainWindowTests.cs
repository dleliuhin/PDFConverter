using System;
using PDFConverter;
using Microsoft.VisualStudio.TestTools.UnitTesting;

namespace PDFConverter.Tests
{
    [TestClass()]
    public class MainWindowTests
    {
        [TestMethod()]
        public void ConvertDemo_Test()
        {
            /*
            MainWindow.ConvertDemo();

            string path = null;

            Type type = Type.GetType("IsPublic");

            HeaderToImageConverter imageConverter = new HeaderToImageConverter();

            System.Globalization.CultureInfo info = new System.Globalization.CultureInfo("info");

            imageConverter.Convert(path, type, 1, info);*/
        }
    }
}
